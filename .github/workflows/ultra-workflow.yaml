name: CI
on:
  push:
  workflow_dispatch:

jobs:
  testing-my-gh-action-yo:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v6
      - name: installing dependencies and building
        run: mvn clean package -DskipTests
      - name: running tests
        run: mvn test
